{
  "name": "meera-drafts-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Telegram bot that turns Meera's notes into on-voice post drafts using Gemini. Hosted on Vercel.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "set-webhook": "node --env-file=.env scripts/set-webhook.js",
    "webhook-info": "node --env-file=.env scripts/webhook-info.js",
    "test-draft": "node --env-file=.env scripts/test-draft.js"
  }
}
